'use client'
import React, { useEffect } from "react";
import OutletConfig from "./OutletConfig";

export default function Sync(props: any) {
    return (
        <React.Fragment>
            <OutletConfig />
        </React.Fragment>
    );
}